function Greeting(age , address) // Parameter
{
    var name = prompt("Enter your name: ");
    alert("Hi "+ name +" Your age is " + age + " You live in " + address);
    // Fuctions use nickname of the code
}

// Calling the function
Greeting(18 , "Kolkata"); // Argument

